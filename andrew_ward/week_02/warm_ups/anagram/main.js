// Given "listen" and a list of candidates like "enlists" "google" "inlets" "banana" the program should return a list containing "inlets".

// Write a javascript file that solves this problem!

//                                NOTES
// return valid words
// sort letters alphabetically
// see if sorted letters match sorted input
// 

var anagram = {

  anagramArray: ['enlists', 'google', 'inlets', 'banana'],

  findAnagram: function (word,anagram.anagramArray) {
    console.log(word);
    console.log('Words to be tested: ' + anagram.anagramArray);

  }

  sortAlphabetically: function (word) {
    var alpha = word.sort();
    return alpha;
  },

  arraysEqual: function (word) {}
  for (var i = 0; i<anagramArray; i++) {
    if (anagramArray[i] === sortAlphabetically(word)) {
      return true
    }
  }
};


























